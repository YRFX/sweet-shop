<view class="tabbar">
  <view 
    wx:for="{{list}}" 
    wx:key="index" 
    class="item" 
    bindtap="switchTab" 
    data-index="{{index}}" 
  >
    <text class="{{selected === index ? 'active' : ''}}">{{item.text}}</text>
  </view>
</view>